## densem (Denotational Semantics)

A denotational semantics intepreter in Haskell for an dynamically typed language, <img src="https://render.githubusercontent.com/render/math?math=WHILE^{cons}">

```math
\[
C ::= skip | x := E | C_0 ; C_1
\]
```